# Optional: canonical site URL for absolute links in production (if your host injects it, you can leave unset locally)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Optional: override direct-sale USD price for Stripe checkout (defaults to PRICING.directPaperbackUsd in code, currently 44)
# DIRECT_SALE_PRICE_USD=44
